Bootstrap: docker
From: python:3.11-slim

%post
  apt-get update && apt-get install -y --no-install-recommends \
      build-essential gcc && \
      rm -rf /var/lib/apt/lists/*
  pip install --no-cache-dir pandas cooler

%environment
  export PYTHONUNBUFFERED=1

